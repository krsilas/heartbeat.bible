export default function SectionHeader(props) {
    return (
        <div className="uppercase -mb-3 text-2xl font-sans font-extrabold pt-4 text-gray-800 dark:text-gray-200">{props.data.text}</div>
    )
}