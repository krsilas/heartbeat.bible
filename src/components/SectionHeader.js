export default function SectionHeader(props) {
    return (
        <div className="uppercase -mb-2 text-2xl font-sans font-extrabold pt-8 text-gray-800 dark:text-gray-200">{props.data.text}</div>
    )
}